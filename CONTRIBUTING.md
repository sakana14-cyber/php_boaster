# Git運用ルール

AIが開発作業でgitを使うときのデフォルト規約。プロジェクトに既存の規約
（CONTRIBUTING.md等）があればそちらを優先する。

## ブランチ

```
main          常にデプロイ可能な状態。直接コミットしない
 └ dev        統合ブランチ。作業はここから分岐しここへ戻す
    └ type/xxx  作業ブランチ
```

- 作業前に `dev` から作業ブランチを切る
- 完了したら `dev` へ `-no-ff` でマージし、作業ブランチは削除する
- `dev` の変更は適宜 `main` へ `-no-ff` でマージする
- `-no-ff` にする理由: マージコミットを残し、後から「この機能はどの範囲の
コミット群か」を追えるようにするため

### 流れ

```bash
git switch dev
git switch -c feat/xxx
# 作業してコミット
git switch dev
git merge --no-ff feat/xxx
git branch -d feat/xxx
```

### ブランチ名

`種別/内容` の形。種別はコミットタグと同じ語を使う。英小文字とハイフン
区切り。日本語は使わない（コマンド・URLで扱いづらいため）。

例: `feat/user-avatar-upload` `fix/login-redirect-loop` `ui/sidebar-hover`

1ブランチ＝1つのまとまった作業。関連する一連の調整はまとめてよい。

## コミット

### メッセージの形

```
<タグ>: 1行で何をしたか

なぜそうしたのかを本文に書く。挙動が変わるなら変更前後を、検証した
ことがあれば併記する。
```

- 1行目は50文字程度まで。末尾に句点は付けない
- 本文は**なぜ**を書く。何をしたかは差分で分かるが、なぜは書かないと消える
- 1つの変更が終わるたびにコミットする。まとめてから分離しようとすると、
同じ行に複数の変更が混ざり分離できなくなる

### タグ

| タグ | 意味 |
| --- | --- |
| `feat` | 新機能・挙動の追加や変更 |
| `fix` | バグ修正 |
| `ui` | 見た目だけの変更（動きは変わらない） |
| `refactor` | 仕様を変えない整理 |
| `docs` | ドキュメントのみ |
| `chore` | 設定・依存関係・雑務 |
| `test` | テストの追加・修正 |
| `perf` | パフォーマンス改善 |
| `revert` | 変更の取り消し |

スコープ（`feat(auth):` のような書き方）は使わない。

判断の目安:

- 動きが増えた・変わった → `feat`
- 意図どおり動いていなかったのを直した → `fix`
- 動きは同じで見た目だけ変わった → `ui`
- 動きも見た目も同じ → `refactor`

似た候補は統合する。`style`/`design`→`ui`、`config`/`env`/`build`/`ci`→
`chore`、`types`→`refactor`。`wip`は使わない（履歴に残すべきでない。中断は
`git stash`、後で整理するなら`git rebase -i`）。

### 例

```
feat: ユーザーアイコンのアップロード機能を追加

プロフィール編集でアイコンを差し替えられるようにした。5MB超は
アップロード前にクライアント側で弾く。

fix: ログイン後のリダイレクトループを直す

未確認メールのユーザーがログインすると /verify と /home を無限に
往復していた。ミドルウェアの判定順序が逆だった。

ui: サイドバー項目にホバーを追加

inline styleのままでは:hoverが書けないため、globals.cssのクラスへ移した。
```

## 使い方

新しいプロジェクトでこのファイルを読み込んだAIは、上記をデフォルトの
git運用として使う。プロジェクトに既存の規約があればそちらを優先し、
矛盾する場合はそちらに従う。
